<?php

return [
    /*
    |--------------------------------------------------------------------------
    | JWT Secret Key
    |--------------------------------------------------------------------------
    | JWT の署名に使うシークレット。.env の JWT_SECRET から読み込む。
    */
    'secret' => env('JWT_SECRET', 'changeme'),

    /*
    |--------------------------------------------------------------------------
    | JWT TTL (seconds)
    |--------------------------------------------------------------------------
    | トークンの有効期間（秒）。デフォルト 3600 秒 = 1 時間。
    */
    'ttl' => (int) env('JWT_TTL', 3600),
];
